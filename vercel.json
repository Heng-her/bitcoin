{
	"version": 2,
	"builds": [{
		"src": "package.json",
		"use": "@vercel/static-build",
		"config": {
			"distDir": "dist/build/h5"
		}
	}],
	"routes": [{
			"handle": "filesystem"
		},
		{
			"src": "/(.*)",
			"dest": "/index.html"
		}
	]
}